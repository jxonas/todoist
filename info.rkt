#lang info

(define collection 'multi)
(define version "0.0")

(define deps
  (list "base"))

(define build-deps
  (list "rackunit-lib"
        "scribble-lib"
        "racket-doc"))

(define pkg-authors '(jxonas))
